# Example environment variables for mycowbot (do NOT commit secrets)
# Copy this file to `.env` and fill the values before running locally.

# Perplexity / OpenClaw provider
OPENCLAW_PERPLEXITY_API_KEY=pplx-REPLACE_ME

# Optional: other provider keys
# OPENCLAW_OTHER_PROVIDER_KEY=your_other_key_here
